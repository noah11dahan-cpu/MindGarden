services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mindgarden
      POSTGRES_PASSWORD: mindgarden
      POSTGRES_DB: mindgarden
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mindgarden -d mindgarden"]
      interval: 5s
      timeout: 5s
      retries: 20

  api:
    build: .
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    # IMPORTANT: no host port exposure
    expose:
      - "8001"

  worker:
    build: .
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    command: ["sh", "-c", "while true; do python -m app.daily_insights_worker; sleep 300; done"]
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - api
    # IMPORTANT: no host port exposure
    expose:
      - "5173"
    environment:
      - VITE_API_BASE=/api
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]

  proxy:
    image: caddy:2-alpine
    ports:
      - "8080:8080"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      - api
      - frontend

volumes:
  postgres_data:
